# Bybit Realtime Chart

Приложение для **мониторинга свечей Bybit Spot в реальном времени** с построением графика и линии справедливой цены на основе **фильтра Калмана**.

---

## Что делает программа

1. Загружает последние **100 минутных свечей** через REST API Bybit.
2. Подключается к **WebSocket** и получает live-обновления текущей свечи.
3. Строит интерактивный график: **свечи**, **объём**, **SMA 5/10**, **линия Калмана**.
4. Отправляет обновления в браузер через **Socket.IO** без перезагрузки страницы.

Пока минута не закрыта — свеча меняется по данным биржи. Когда приходит `confirm: true` — свеча фиксируется, фильтр Калмана делает один шаг.

---

## Архитектура

```
                         ┌─────────────────┐
                         │   Bybit Spot    │
                         │  REST + WS v5   │
                         └────────┬────────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │                           │
              REST (история)            WebSocket (live kline)
                    │                           │
                    └─────────────┬─────────────┘
                                  ▼
                        ┌──────────────────┐
                        │ data_collector   │  ← свечи, current_candle, Калман
                        └────────┬─────────┘
                                 │
                                 ▼
                        ┌──────────────────┐
                        │  chart_builder   │  ← Plotly JSON
                        └────────┬─────────┘
                                 │
                                 ▼
                        ┌──────────────────┐
                        │   web_server     │  ← Flask + SocketIO
                        └────────┬─────────┘
                                 │
                                 ▼
                        ┌──────────────────┐
                        │  Браузер (UI)    │  ← Plotly.js
                        └──────────────────┘
```

### Модули

| Файл | Роль |
|------|------|
| `main.py` | Точка входа, запуск WebSocket-потока и веб-сервера |
| `config.py` | Пара, таймфрейм, эндпоинты Bybit, параметры Калмана |
| `data_collector.py` | Сбор данных, хранение свечей, фильтр Калмана |
| `kalman_filter.py` | Оценка «справедливой цены» (уровень + тренд) |
| `chart_builder.py` | Генерация графика Plotly на сервере |
| `web_server.py` | HTTP API, SocketIO, broadcast обновлений |
| `template_manager.py` | Загрузка HTML-шаблонов |
| `templates/index.html` | Веб-интерфейс |

### Потоки

- **Главный поток** — Flask/SocketIO (порт `5000`)
- **Фоновый поток** — WebSocket-клиент к Bybit (asyncio)

---

## Технологии

| Категория | Стек |
|-----------|------|
| Backend | Python 3, Flask, Flask-SocketIO, asyncio, threading |
| Биржа | Bybit API v5 (Spot) — REST + WebSocket |
| Аналитика | NumPy, фильтр Калмана (локальный линейный тренд) |
| Графики | Plotly (сервер) + Plotly.js (клиент) |
| Frontend | HTML, CSS, JavaScript, Socket.IO |

---

## Быстрый старт

### Зависимости

```bash
pip install flask flask-socketio plotly websockets requests numpy certifi urllib3
```

### Запуск

```bash
python main.py
```

Откройте в браузере: **http://localhost:5000**

### Остановка

- Кнопка **«Остановить»** в веб-интерфейсе
- Или **Ctrl+C** в консоли

---

## Настройки

Основные параметры в `config.py`:

| Параметр | Значение | Описание |
|----------|----------|----------|
| `SYMBOL` | `BTCUSDT` | Торговая пара |
| `TIMEFRAME` | `1` | Таймфрейм в минутах |
| `MAX_CANDLES` | `100` | Свечей на графике |
| `FLASK_PORT` | `5000` | Порт веб-сервера |
| `ENABLE_SMA_5/10` | `True` | Скользящие средние |
| `KALMAN_*` | см. config | Параметры фильтра Калмана |

---

## Как определяется закрытие свечи

Bybit присылает в push-сообщении kline поле **`confirm`**:

- `false` — свеча ещё формируется
- `true` — минута закрыта, OHLCV финальные

Ключ свечи — поле **`start`** (время открытия минуты), а не `timestamp` (время последней сделки).

---

## Результаты

- Live-график минутных свечей Bybit Spot, согласованный с данными биржи
- Обновление текущей свечи до закрытия минуты
- Линия справедливой цены (Калман) — один шаг на каждую **закрытую** свечу
- Автопереподключение WebSocket при обрыве связи
- Статистика 24ч (high, low, volume) в интерфейсе

---

## Ограничения

- Рынок **Spot** (не Linear/Inverse фьючерсы)
- Подписка на **kline**, не на order book
- Kline push от Bybit — с интервалом ~1–60 сек, не на каждое изменение стакана
- Линия Калмана и SMA — дополнительные индикаторы, их нет на стандартном UI Bybit

---

## Дополнительная документация

Подробная текстовая сводка: [`Сводка_проекта.txt`](Сводка_проекта.txt)

---

## Лицензия

Учебный / pet-project.
